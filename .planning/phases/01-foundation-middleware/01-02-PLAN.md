---
phase: 01-foundation-middleware
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.ts
  - src/config/openai.ts
  - src/config/logger.ts
  - src/db/prisma.ts
  - src/utils/token.ts
  - src/utils/sanitize.ts
  - src/utils/hash.ts
  - src/errors/index.ts
  - src/index.ts
  - src/server.ts
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "Server crashes on startup if DATABASE_URL or OPENAI_API_KEY are missing, with clear error messages"
    - "A typed env object is the only way to access environment variables throughout the app"
    - "Custom error classes exist for all error types with correct HTTP status codes"
    - "Utility functions generate tokens, sanitize input, and hash IPs correctly"
    - "Prisma client connects with graceful shutdown on SIGTERM/SIGINT"
    - "OpenAI client singleton is configured from env"
  artifacts:
    - path: "src/config/env.ts"
      provides: "Zod-validated typed env object"
      exports: ["env"]
    - path: "src/config/openai.ts"
      provides: "OpenAI client singleton"
      exports: ["openai"]
    - path: "src/config/logger.ts"
      provides: "Pino logger instance"
      exports: ["logger"]
    - path: "src/db/prisma.ts"
      provides: "Prisma client singleton with graceful shutdown"
      exports: ["prisma"]
    - path: "src/errors/index.ts"
      provides: "Custom error classes"
      exports: ["AppError", "NotFoundError", "ValidationError", "RateLimitError", "AIError"]
    - path: "src/utils/token.ts"
      provides: "Nanoid token generation"
      exports: ["generateToken"]
    - path: "src/utils/sanitize.ts"
      provides: "Input sanitization"
      exports: ["sanitizeInput"]
    - path: "src/utils/hash.ts"
      provides: "SHA-256 IP hashing"
      exports: ["hashIp"]
  key_links:
    - from: "src/index.ts"
      to: "src/config/env.ts"
      via: "import at top before all other modules"
      pattern: "import.*env"
    - from: "src/server.ts"
      to: "src/config/env.ts"
      via: "uses env.PORT"
      pattern: "env\\.PORT"
    - from: "src/db/prisma.ts"
      to: "src/config/env.ts"
      via: "uses env.DATABASE_URL"
      pattern: "env\\.DATABASE_URL"
---

<objective>
Set up environment validation, client singletons (Prisma, OpenAI, Pino), custom error classes, and utility functions.

Purpose: These are the shared building blocks every service and middleware in the application imports. Environment validation ensures fail-fast behavior. Singletons prevent resource leaks. Error classes standardize error handling. Utilities provide reusable helpers.
Output: Fully typed config, client singletons, error hierarchy, and utility functions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-middleware/01-CONTEXT.md
@src/app.ts
@src/index.ts
@src/server.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment validation, client singletons, and Pino logger</name>
  <files>src/config/env.ts, src/config/openai.ts, src/config/logger.ts, src/db/prisma.ts, src/index.ts, src/server.ts</files>
  <action>
**src/config/env.ts:**
Create a Zod schema that validates all environment variables. Import `z` from `zod`. Define the schema:
- DATABASE_URL: z.string().url() — required, no default
- OPENAI_API_KEY: z.string().min(1) — required, no default
- PORT: z.coerce.number().default(3001)
- NODE_ENV: z.enum(["development", "production", "test"]).default("development")
- CORS_ORIGINS: z.string().default("http://localhost:3000") (comma-separated, split into array in a computed field or leave as string for cors config)
- REPORT_EXPIRY_DAYS: z.coerce.number().default(90)
- OPENAI_MODEL: z.string().default("gpt-4o")
- OPENAI_MAX_TOKENS: z.coerce.number().default(6000)
- DAILY_SPEND_LIMIT_CENTS: z.coerce.number().default(5000) (50 dollars)
- LOG_LEVEL: z.enum(["fatal","error","warn","info","debug","trace"]).default("info")

Parse `process.env` through the schema. If validation fails, log all errors clearly and call `process.exit(1)`. Export the validated result as `export const env = ...`. Also export the inferred type: `export type Env = z.infer<typeof envSchema>`.

**src/config/openai.ts:**
Import `OpenAI` from `openai` — but first check if openai is in package.json. If NOT installed, the task action should run `npm install openai` first. Create and export a singleton: `export const openai = new OpenAI({ apiKey: env.OPENAI_API_KEY })`. Import env from `./env`.

**src/config/logger.ts:**
Import `pino` from `pino`. Create logger with: level from `env.LOG_LEVEL`, transport in development uses `pino-pretty` (check if installed, if not add as devDependency: `npm install -D pino-pretty`), JSON format in production (default pino behavior). Export as `export const logger = pino(options)`.

**src/db/prisma.ts:**
Import `PrismaClient` from `../generated/prisma`. Create singleton pattern (check for existing instance on globalThis in development to prevent hot-reload issues). Register graceful shutdown: on SIGTERM and SIGINT, call `prisma.$disconnect()` then `process.exit(0)`. Export as `export const prisma = ...`.

**src/index.ts:**
Update to: first line `import 'dotenv/config'`, second line `import './config/env'` (this triggers validation), then `import './server'`. The env import MUST come before server import to ensure fail-fast.

**src/server.ts:**
Update to use `env.PORT` instead of `process.env.PORT || 3001`. Import `env` from `./config/env` and `logger` from `./config/logger`. Replace `console.log` with `logger.info`.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/PMF\ TOOL/BE && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles without errors. env.ts exports typed env object. openai.ts exports OpenAI client. logger.ts exports Pino logger. prisma.ts exports Prisma client with shutdown hooks. index.ts validates env before server start.</done>
</task>

<task type="auto">
  <name>Task 2: Custom error classes and utility functions</name>
  <files>src/errors/index.ts, src/utils/token.ts, src/utils/sanitize.ts, src/utils/hash.ts</files>
  <action>
**src/errors/index.ts:**
Create an error class hierarchy. All extend from a base `AppError`:

```
export class AppError extends Error {
  constructor(
    public message: string,
    public statusCode: number = 500,
    public code: string = 'INTERNAL_ERROR',
    public details?: Array<{ field: string; message: string }>
  ) {
    super(message);
    this.name = this.constructor.name;
  }
}

export class ValidationError extends AppError {
  constructor(message: string, details?: Array<{ field: string; message: string }>) {
    super(message, 400, 'VALIDATION_ERROR', details);
  }
}

export class NotFoundError extends AppError {
  constructor(message: string = 'Resource not found') {
    super(message, 404, 'NOT_FOUND');
  }
}

export class RateLimitError extends AppError {
  constructor(message: string = 'Rate limit exceeded') {
    super(message, 429, 'RATE_LIMIT_EXCEEDED');
  }
}

export class AIError extends AppError {
  constructor(message: string = 'AI service error') {
    super(message, 502, 'AI_ERROR');
  }
}
```

**src/utils/token.ts:**
Install nanoid v3 (NOT v4+ which is ESM-only, incompatible with CommonJS): `npm install nanoid@3`. Import `nanoid` from `nanoid`. Export `generateToken` function that returns a 21-character URL-safe token: `export const generateToken = (): string => nanoid(21)`.

**src/utils/sanitize.ts:**
Export `sanitizeInput` function that: (1) strips all HTML tags using a regex `/<[^>]*>/g` replaced with empty string, (2) trims whitespace, (3) truncates to 500 characters. Signature: `export const sanitizeInput = (input: string): string => ...`. Handle null/undefined gracefully by returning empty string.

**src/utils/hash.ts:**
Import `createHash` from Node's built-in `crypto` module. Export `hashIp` function that takes an IP string and returns SHA-256 hex digest. Use a static salt prefix (e.g., `"pmf-insights-ip-salt:"`) concatenated with the IP before hashing, so rainbow tables don't work. Signature: `export const hashIp = (ip: string): string => ...`.
  </action>
  <verify>
    <automated>cd /Users/apple/Desktop/PMF\ TOOL/BE && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All error classes exported from src/errors/index.ts with correct HTTP status codes. generateToken produces 21-char strings. sanitizeInput strips HTML and truncates to 500 chars. hashIp returns SHA-256 hex digest. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All exports are properly typed
- Error classes map to correct HTTP codes: ValidationError=400, NotFoundError=404, RateLimitError=429, AIError=502
- nanoid is v3.x in package.json (not v4+)
</verification>

<success_criteria>
Server boots with validated environment (or crashes clearly on missing required vars). All singletons, error classes, and utilities are importable and correctly typed. No `process.env` access outside of env.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-middleware/01-02-SUMMARY.md`
</output>
